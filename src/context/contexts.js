import { createContext } from "react";

/**
 * Auth Context - stores authentication state
 */
export const AuthContext = createContext(null);

/**
 * Task Context - stores task management state
 */
export const TaskContext = createContext(null);
